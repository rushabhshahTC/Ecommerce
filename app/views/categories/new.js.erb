$("#modal-window").find(".modal-content").html("<%= j (render 'new') %>");
$("#modal-window").modal();


    $("#new_category").validate({
        rules: {
        "category[name]": "required"
        }
    });
